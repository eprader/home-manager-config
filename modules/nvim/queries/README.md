As of right now `nvim-treesitter` is archived and while treesitter has been upstreamed, queries for highlighting and the like for specific languages are not.
possible [this issue](https://github.com/neovim/neovim/issues/39006) would eventually upstream queries to a central repo. Untill then queries need to be maintained manually.

These queries were cloned from the `nvim-treesitter` archived repository.
